export enum ComparisonView {
    avg = 'avg',
    expected = 'expected',
    personal = 'personal',
    best = 'best'
}

export enum FooterBarView {
    start = 'start',
    normal = 'normal',
    reset = 'reset'
}